# 할 일 목록

> 끝난 항목은 여기서 지우고 커밋 로그(또는 docs/worklog.md)로 옮긴다. 완료 표시를 쌓지 않는다.
> 항목마다 왜 / 어느 파일 / 어떤 방향 세 줄 — 맥락 없이 열어도 착수 가능하게.

## 1. 여성·청년·복지 섹션 켜기 (비용 걸림돌 해소됨)

- **왜**: 켜기로 결정됨(2026-08-08). **2026-08-11 확인: 비용 걸림돌은 없었음** — 꺼진 섹션 기사도 이미 요약비를 내고 있었고, 같은 날 요약 범위를 발송 대상만으로 좁혀 그 낭비를 제거함. 섹션을 켜도 비용은 거의 안 오름.
- **어느 파일**: `src/section_builder.py`의 `PHASE1_ACTIVE`에 3, 4, 5 추가 (한 줄).
- **어떤 방향**: 켜고 커밋 → 1주 뒤 발송량 보고. 주의: 회당 발송이 약 50건에서 약 80건으로 늘어 읽는 사람 부담 커짐. min_importance(현 4점)를 5점으로 올려 분량을 조절할지와 함께 결정.

## 2. 이메일 발송 흔적 정리

- **왜**: 발송 채널을 텔레그램 하나로 확정(2026-08-08 결정). 빈 껍데기 코드와 설정 예시가 남아 있으면 다음 세션 AI가 "구현해야 하나?" 헷갈림.
- **어느 파일**: `src/email_send.py`(삭제), `pyproject.toml`의 email 관련 부속 항목, `.env.example`의 RESEND·EMAIL 줄, `config/recipients.yaml.example`, `templates/` 빈 폴더, `CLAUDE.md`의 이메일 언급.
- **어떤 방향**: 전부 지우되, CLAUDE.md 수정 시 관련 문장만 최소 수정. 지우기 전에 참조하는 곳이 없는지 검색으로 확인.

## 3. 빈손 수집처 수리

- **왜**: 프레시안·참세상 RSS 0건, 구글알리미 3개(경상남도노동·경상남도정치·정의당경남) 0건, MBC경남 스크래핑 시간초과 잦음, 민중의소리 본문 가져오기(Jina) 실패 잦음 — 수집 구멍이 생기면 브리핑 품질 저하. 경남일보는 복구됨(50건). KNN은 본문 가져오기 정상.
- **어느 파일**: `config/feeds.yaml`(RSS 주소), `config/scrape.yaml`(스크래핑), `src/scrape.py`.
- **어떤 방향**: 각 매체 RSS 주소가 바뀌었는지 실제 접속으로 확인 → 죽은 주소는 교체하거나 스크래핑으로 전환 → 못 살리는 곳은 feeds.yaml에서 빼고 사유를 주석으로 남김.

## 4. Phase 3~4 재정의

- **왜**: 원본 기획서 소실로 Phase 3~4 내용을 모름(PRD 참조). 로드맵 끝이 비어 있음.
- **어느 파일**: `PRD.md`의 4장.
- **어떤 방향**: 레고님과 대화로 다음 목표를 정한 뒤 PRD 갱신. 후보: 주간 요약판, 수신자 피드백 반영, 분류 품질 개선.

## 5. 비용 안전장치 수리

- **왜**: "월 $30 넘으면 요약 기준을 자동 올린다"는 규칙이 고장 남 — 올리는 값(4점)과 원래 값(4점)이 같아 작동 안 함. 2026-08-11 현재 월 $38~40 추세라 실제로 필요한 장치.
- **어느 파일**: `src/summarize.py`의 `IMPORTANCE_THRESHOLD_HIGH`(현재 4)를 5로 올리거나, 월 비용 한도 초과 시 다른 조치(발송 기준 상향 등)로 교체.
- **어떤 방향**: 임계값만 5로 올리면 가장 간단. 단, 정기 실행에서 실제로 발동하는지(월 $30 도달 시점) 확인 필요.
