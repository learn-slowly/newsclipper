# 할 일 목록

> 끝난 항목은 여기서 지우고 커밋 로그(또는 docs/worklog.md)로 옮긴다. 완료 표시를 쌓지 않는다.
> 항목마다 왜 / 어느 파일 / 어떤 방향 세 줄 — 맥락 없이 열어도 착수 가능하게.

## 1. 비용 조절 + 여성·청년·복지 섹션 켜기 (묶어서)

- **왜**: 8월 비용 추세가 월 $35로 목표($30) 초과 조짐인데, 섹션 3개를 더 켜면 $40~45까지 갈 수 있음. 켜기로 결정(2026-08-08, PRD 참조)했으니 비용 절감과 한 묶음으로 진행해야 안전.
- **어느 파일**: `src/section_builder.py`의 `PHASE1_ACTIVE`에 3, 4, 5 추가. 비용 조절은 `config/keywords.yaml`의 `min_importance`(현재 4) 또는 수집처 축소.
- **어떤 방향**: 먼저 최근 2주 실행 기록으로 "5점만 발송하면 몇 건이 남나" 시뮬레이션 → 기준 상향이 과하면 다른 수단(구글 알리미 정리 등) 검토 → 섹션 켜고 1주일 비용 관찰.

## 2. 이메일 발송 흔적 정리

- **왜**: 발송 채널을 텔레그램 하나로 확정(2026-08-08 결정). 빈 껍데기 코드와 설정 예시가 남아 있으면 다음 세션 AI가 "구현해야 하나?" 헷갈림.
- **어느 파일**: `src/email_send.py`(삭제), `pyproject.toml`의 email 관련 부속 항목, `.env.example`의 RESEND·EMAIL 줄, `config/recipients.yaml.example`, `templates/` 빈 폴더, `CLAUDE.md`의 이메일 언급.
- **어떤 방향**: 전부 지우되, CLAUDE.md 수정 시 관련 문장만 최소 수정. 지우기 전에 참조하는 곳이 없는지 검색으로 확인.

## 3. 빈손 수집처 수리

- **왜**: 경남일보·프레시안·참세상 RSS가 최근 0건, KBS경남 0건, KNN 본문 가져오기 실패 잦음 — 수집 구멍이 생기면 브리핑 품질 저하.
- **어느 파일**: `config/feeds.yaml`(RSS 주소), `config/scrape.yaml`(스크래핑), `src/scrape.py`.
- **어떤 방향**: 각 매체 RSS 주소가 바뀌었는지 실제 접속으로 확인 → 죽은 주소는 교체하거나 스크래핑으로 전환 → 못 살리는 곳은 feeds.yaml에서 빼고 사유를 주석으로 남김.

## 4. Phase 3~4 재정의

- **왜**: 원본 기획서 소실로 Phase 3~4 내용을 모름(PRD 참조). 로드맵 끝이 비어 있음.
- **어느 파일**: `PRD.md`의 4장.
- **어떤 방향**: 레고님과 대화로 다음 목표를 정한 뒤 PRD 갱신. 후보: 주간 요약판, 수신자 피드백 반영, 분류 품질 개선.
