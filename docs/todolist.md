# 할 일 목록

> 끝난 항목은 여기서 지우고 커밋 로그(또는 docs/worklog.md)로 옮긴다. 완료 표시를 쌓지 않는다.
> 항목마다 왜 / 어느 파일 / 어떤 방향 세 줄 — 맥락 없이 열어도 착수 가능하게.

## 1. 여성·청년·복지 섹션 켜기 (걸림돌 모두 해소됨)

- **왜**: 켜기로 결정됨(2026-08-08). 비용 걸림돌 없음(2026-08-11 확인 — 꺼진 섹션 기사도 이미 요약비를 내고 있었고 그 낭비는 제거함). 분량 걸림돌도 없음(2026-08-11 회당 30건 상한 도입 — 섹션을 켜도 총량은 30건 그대로).
- **어느 파일**: `src/section_builder.py`의 `PHASE1_ACTIVE`에 3, 4, 5 추가 (한 줄).
- **어떤 방향**: 켜고 커밋 → 1주 뒤 섹션별 분포 확인. 켜면 30건 안에서 섹션끼리 자리를 다투므로, 여성·청년·복지 기사가 노동·기후를 밀어낼 수 있음. 밀림이 심하면 `max_items`를 올리거나(현 30) 섹션별 최소 보장을 검토.

## 2. jpnews 기존 논평 백필 (2,615건 남음)

- **왜**: jpnews 구글시트를 6열로 전환했지만 기존 6,720건 중 약 4,100건만 제목·주제가 채워짐. 나머지 2,615건은 빈칸이라 논평 매칭에 잡히지 않음.
- **어느 파일**: `/Volumes/4t1/coding-archive/jpnews/scripts/backfill_classify.py`
- **어떤 방향**: `python -u scripts/backfill_classify.py --run --yes`로 실행 (약 30분). 순번 ID + 실패 건 1건 재시도 + 유형 정규화 적용됨. 시트 읽기에 시간이 걸리므로 끊기면 재실행.

## 3. 비용 안전장치 수리

- **왜**: "월 $30 넘으면 요약 기준을 자동 올린다"는 규칙이 고장 남 — 올리는 값(4점)과 원래 값(4점)이 같아 작동 안 함. 2026-08-11 현재 월 $38~40 추세라 실제로 필요한 장치.
- **어느 파일**: `src/summarize.py`의 `IMPORTANCE_THRESHOLD_HIGH`(현재 4)를 5로 올리거나, 월 비용 한도 초과 시 다른 조치(발송 기준 상향 등)로 교체.
- **어떤 방향**: 임계값만 5로 올리면 가장 간단. 단, 정기 실행에서 실제로 발동하는지(월 $30 도달 시점) 확인 필요.
